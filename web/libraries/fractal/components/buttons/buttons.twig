{% if buttons|length %}
  <div class="buttons buttons--{{ align|default('left') }}">
  {% for button in buttons %}
      {% include '@fractal/button/button.twig' with {
        text: button.text,
        url: button.url,
        style: button.style
      } %}
    {% endfor %}
  </div>
{% endif %}
