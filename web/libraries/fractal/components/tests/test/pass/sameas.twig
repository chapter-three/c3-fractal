{% set f = false %}
{% set t = true %}
{% set n = null %}
{% set a = [] %}
{% set o = {} %}
{% set e = '' %}

{% if f is same as(false) %}
  <code>f</code> really is the 'false' PHP value. <br>
{% endif %}


{% if t is same as(false) %}
  <code>t</code> really is the 'false' PHP value. <br>
{% endif %}

{% if n is same as(false) %}
  <code>n</code> really is the 'false' PHP value. <br>
{% endif %}

{% if a is same as(false) %}
  <code>a</code> really is the 'false' PHP value. <br>
{% endif %}

{% if o is same as(false) %}
  <code>o</code> really is the 'false' PHP value. <br>
{% endif %}

{% if e is same as(false) %}
  <code>e</code> really is the 'false' PHP value. <br>
{% endif %}
